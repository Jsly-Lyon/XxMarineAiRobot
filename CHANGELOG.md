# 更新日志

本项目采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 风格，版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-02

首个版本：面向海洋科研的智能问答平台（品牌名「瀚海知问 AI」）整体入库，并完成前端交互重构与登录/鉴权/数据隔离改造。

### Added（新增）

- **品牌与文案统一**：全站由「小哈 / 欣星 AI 机器人」重塑为「瀚海知问 AI」，欢迎语/空态/占位符/页面标题全部改为海洋科研语境。
- **登录 / 注册一体化弹窗**：无需独立登录页；侧边栏底部、开启新对话、发送消息等入口在未登录时弹出登录/注册弹窗，注册成功自动登录。
- **登录用户信息缓存（Redis）**：登录时把用户信息写入 Sa-Token 会话（Redis），新增 `GET /auth/info` 供前端快速获取昵称/头像。
- **SSE 流式对话鉴权**：`/chat/completion` 改为需登录，且流式请求由前端原生 fetch 显式携带 `Bearer token`。
- **会话数据隔离**：`t_chat` 增加 `user_id` 归属列（含迁移脚本 `V1__chat_user_ownership.sql`），新建/列表/消息/重命名/删除均按当前登录用户过滤。
- **未登录前端收敛**：未登录时左侧「历史对话」整块隐藏、不请求后端；登录后自动加载历史。
- **空态体验**：无历史对话时展示引导图标与「暂时无历史对话」。

### Changed（优化）

- 对话页改为 **ChatGPT 式居中布局**：消息与输入框集中在屏幕中间一列，不再通栏铺满。
- 输入框（`ChatInputBox`）能力增强：**模型下拉/联网搜索开关、输入自动增高、回车发送、空内容禁用发送按钮、跟随输入值同步高度**，模型下拉改为向上展开避免被屏幕底部裁切。
- 页脚「内容由 AI 生成，请仔细甄别」由悬浮底栏改为**紧随输入框下方**显示。
- 首页→聊天页的首条消息改为**隐式传递**（sessionStorage 与会话 uuid 绑定），不再经过 URL，规避超长消息的 URL 长度限制。
- 侧边栏：登录后显示头像昵称与退出入口；登出/切换账号自动回首页，避免停留在他人会话页。

### Fixed（修复）

- 修复跳转聊天页整页白屏（漏导 `Layout` 组件导致无法解析）。
- 修复登录/注册时 IDE 对新增 VO（`RenameChatReqVO`、`DeleteChatReqVO`）的通配导入解析问题，改为显式导入。
- 修复切换账号后继续请求旧会话触发「此对话不存在」的体验问题。
- 空内容发送不再弹多余提示，统一为按钮禁用态。

### Security（安全）

- Sa-Token 拦截规则收紧：除 `/auth/login`、`/auth/register`、`/error` 外**全部接口需登录**（移除此前对 `/chat/**` 的放行）。
- 会话归属校验统一语义：非本人 uuid 一律返回「此对话不存在」，不暴露其是否存在。
- 未登录由全局异常统一返回 HTTP 401，SSE 场景不再二次报 `MediaTypeNotAcceptable`。
- 含模型 API Key / 数据库 / Redis 口令的配置文件全部移出版本库，仓库仅保留脱敏示例（`application-dev.example.yml`）。
